% Scope-setting, NOT results: two-three sentences plus a footnote (paper-roadmap rule).
% E1: no availability gap (0/24 extents, median freshness 2 days).
% E2: no currency advantage (ABSENT, +0.008 +- 0.031 px).
% Footnote: scene-per-year counts are right-censored lower bounds; the censoring does
% not touch gap rate or freshness. E2's null is also consistent with OSM not having
% recorded the change (open-items 21) -- state honestly.
% E3 does not appear here or anywhere in the letter (corrections-log entry 16).

TODO.
