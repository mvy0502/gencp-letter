% Method: the 2x2 loss factorial (GAN x L1/LPIPS), the evaluation instrument (KARIOS),
% the reference construction, and the registration discipline (predictions and
% falsification bands committed before each run).
% One paragraph on the 1/256 scale bug plus a repo pointer -- it is a methods note here,
% not a result.
% Seed defence in one sentence: pre-registered n >= 60 single-draw rule, regA det/stoch
% bound <= 0.05 px, measured effect sizes 0.38-0.70 px.

TODO.
