# Corrections log

Every case where what was registered, claimed, or assumed diverged from what actually happened.
Kept in one place so the pattern is visible: the run is corrected or the record is corrected,
never neither. Companion to the withdrawn-claims list in [../README.md](../README.md).

The test applied throughout, stated once here:

> Would a reader who knew about this discrepancy interpret any result differently?

If no — correct the record, prominently, with the evidence. If yes, or if the discrepancy is
biased **toward** a registered prediction — correct the run. A bias against our own prediction
strengthens the result and may stand (disclosed); a bias toward it must be eliminated even when
probably immaterial, because "probably immaterial and pointing at our prediction" is exactly the
configuration a skeptical reader is entitled to reject.

## Why this log is tiered

A documentation slip, a lost artifact and a misstated registered check are not the same kind of
failure, and grading them identically invites a reader to discount the whole record by its worst
line rather than by its worst tier. The tiers below say which kind each entry is, so that
severity is read from the tier and not from the fact of appearing here at all. Nothing is
softened by the sorting: every entry keeps the wording it was written with, and an entry that
belongs in two tiers is split into two entries rather than filed under the lesser one — which is
what happened to the stop rule, entries 26 and 27.

**Tier 1 — deviations with substantive defences.** The protocol, the run, or the mechanism
diverged from what was registered or assumed. Each carries an argument and a disposition: the
run was corrected, the claim was withdrawn, or the deviation is defended on the evidence and
the defence is written down where it can be attacked.

**Tier 2 — unrecoverable artifacts.** Something needed to re-derive a reported number no longer
exists. These are the entries a sceptical reader should weigh most heavily, because no argument
can repair them, so each states explicitly **which reported numbers are consequently
uncheckable**.

**Tier 3 — reporting errors.** The runs were correct and the artifacts survive; a document
stated something the artifacts do not support. Cheap to fix, and listed anyway, because a
reader who finds one uncorrected is entitled to wonder what else was never checked.

## Drafted, not yet applied

Entries under this heading are **not part of the record** (standing practice 12). A drafted
entry is a claim awaiting review; only an entry in a tier below is a correction that was
made. Drafts are written here and nowhere else, and move to their tier when applied.

*None at present (13 September 2026).*

## Tier 1 — deviations with substantive defences

| # | date | what was claimed/registered | what was actually true | resolution | where |
|---|---|---|---|---|---|
| 2 | 19 Aug | "11 gate chips fail with zero key points; water loss causes it" | Harness bug: references missing for 11 chips, errors silenced by `>/dev/null`; the chips were never evaluated | attribution withdrawn; harness fixed; erratum §6.1 | [renderer-tolerance.md](renderer-tolerance.md) |
| 3 | 19 Aug | Warm-up works because "D trains an order of magnitude faster than it damages G" at 2e-5 | pix2pix has a single `--lr`; the G/D ratio is unchanged. It works because D's task vs a near-fixed G is easy at any rate | justification replaced, config unchanged | [phase-c-config.md](phase-c-config.md) |
| 4 | 19 Aug | Phase C/D scene dates assumed interchangeable | Cappadocia has no usable 04-30 scene; four-week phenology deviation, direction of bias ambiguous | registered as confound before data prepared | [phase-cd-preparation.md](phase-cd-preparation.md) §3 |
| 5 | 19 Aug | "Warm-up = 2 epochs at 2e-5"; then, during triage: "both arms lose one trailing epoch identically — symmetric, therefore neutral" | Warm-up epoch 2 ran at lr = 0 (scheduler off-by-one with `n_epochs_decay=0`). The symmetry claim was **wrong**: main-stage losses are symmetric, but the dead warm-up epoch falls on C1 alone — widening C2's step-budget advantage from 11.9% to 14.7%, toward the registered R1 prediction | **run corrected**: C1 restarted with `--lr_policy step` (stage 1 only), 25-min partial run discarded before any result existed. Symmetric losses kept and disclosed (stock pix2pix behaviour) | [phase-c-config.md](phase-c-config.md) amendment |
| 6 | 19 Aug | Geofabrik chip extracts assumed equivalent to Overpass renders (first fix: a per-extract `strategy` config key) | osmium's default `simple` strategy drops boundary-crossing multipolygons (3:1 forest→background flow, worst chip 84% wrong); the config-key fix was a **no-op** — the key is silently ignored | `-s smart` on the command line; transparency gate re-run to 99.91% class agreement | [renderer-tolerance.md](renderer-tolerance.md) |
| 7 | 19 Aug | Kaggle mount failure "fixed by `dataset_sources` in kernel-metadata.json" | The attachment was never broken; the hard-coded path `/kaggle/input/gencp-tr` was wrong (private datasets can mount under `/kaggle/input/datasets/<owner>/<slug>`, and the layout differs per node) | mount discovery in `train_c1_c2.py`; both layouts observed across the C1/C2 nodes | [phase-c-config.md](phase-c-config.md) |
| 8 | 19 Aug | Kernel identity assumed = metadata `id` | `kernels push` derives the slug from the **title** and only warns; `status` on the requested id returns a permission error that reads like a privacy problem | ids derived from titles in `build_kernels.py` so they cannot disagree | [build_kernels.py](../kaggle/build_kernels.py) |
| 9 | 19 Aug | `torch.cuda.is_available()` accepted as proof the GPU is usable | True on a P100 (sm_60) that torch 2.10+cu128 cannot emit code for; every kernel launch would have failed after preflight passed | preflight asserts `sm_XX ∈ torch.cuda.get_arch_list()`; T4 pinned in metadata | [phase-c-config.md](phase-c-config.md) |
| 11 | 20 Aug | Phase B headline: "the model generalises to Ankara — no measurable geographic penalty" (matched gap −0.247 px); later, Phase D draft wording: "uniform site-level penalty" | Both were baseline artifacts. −0.247 was measured against the 44-chip arm-B European baseline; against the better-supported 568-chip baseline the SAME Ankara results show **+0.226 px** (Q1 +0.26, Q2 +0.29, Q3 +0.38, Q4 +0.17, Q5 +0.04). The registered T4 criterion (Q4/Q5 > +0.5) is still NOT triggered — dense strata carry no substantial penalty — but the blanket "no measurable penalty" does not survive, and the three Turkish sites on one baseline form a **gradient** (+0.23 / +0.45 / +0.51), not a uniform level | headline corrected here and flagged for the coordinator (Report 1 follow-up); Phase D documents write the gradient, not "uniform"; caught by check 1 (re-scoring all sites against one baseline) BEFORE the Phase D document was written | [phase-d-checks-registration.md](phase-d-checks-registration.md) |
| 12 | 20 Aug | Badlands chips at Cappadocia identified by input-render composition (`no_veg ≥ 25%`); the landform row initially discussed against that selection | The rule found 5/130 chips at the site *defined* by that terrain — a measurement failure, and near-circular (the composition raster is the model's own input). Replaced by the registered DEM rule (Copernicus GLO-30 slope-std, labels committed before scoring): n = 33 badlands / 65 flat | no verdict was scored against n = 5; repaired rule gives badlands−flat matched-gap difference −0.013 ± 0.141 px (no morphological signature, now with power) | [phase-d-checks-registration.md](phase-d-checks-registration.md), [dem-ruggedness-labels-36SXJ.csv](dem-ruggedness-labels-36SXJ.csv) |
| 13 | 21 Aug | The evaluation archive and the production path assumed to render from the same OSM source | The Ankara evaluation inputs predate the Geofabrik switch and were rendered from **Overpass**; the tool renders from the dated snapshots. Render content differs on 3.3–7.9% of pixels on the first gate chips, concentrated in forest and water edges | caught by the tool's correctness gate (registered comparison of tool output against archived outputs); gate recorded FAILED-as-designed and re-registered against PBF-rendered references | [tool-gate-registration.md](tool-gate-registration.md), [tool-gate-registration-2.md](tool-gate-registration-2.md) |
| 14 | 21 Aug | Non-reproducible Phase B fakes explained as "generation-environment numerics"; inference described as "the byte-verified path"; gate-2 registration cited `rasteriser/chips/` as the corpus render source | The historical inference command leaves **UNet dropout active at test time** (no `--eval`, no `--no_dropout`) — identical runs differ by 0.1–0.4 px per chip median. **This is not a defect: it is pix2pix's own design** (the paper provides noise "only in the form of dropout, applied at both training and test time" in place of a z vector); we inherited an intended behaviour we did not know about. The conclusion built on the wrong mechanism survives (regen-vs-published +0.034 ± 0.045 — zero-mean at the median level); "byte-verified" is narrowed to **the render layer** (bit-exact, proven); the corpus render source citation was wrong (`acc_clcgate/inputs` are the verified artifacts, `rasteriser/chips` is an older corpus) | mechanism corrected; gate criterion 1's fake layer recorded as unsatisfiable across processes; tool pins a seed (reproducibility without changing the measured distribution) | [tool-gate-registration-2.md](tool-gate-registration-2.md) |
| 15 | 21 Aug | The acceptance corpora assumed to match the current render path (`acc_clcgate`, `acc_clcheld` = the +0.012 px held-out gate corpus) | **20/30 and 17/25 respectively are stale pre-fix renders** (dominant flow light_green→forest — the simple-strategy multipolygon deficit; German chips carry an extra snapshot-drift caveat). **The rasteriser acceptance figure +0.012 ± 0.132 px is not quotable until the gate is re-run on regenerated inputs**; every document quoting it now carries a PENDING flag | census committed (`tool_runs/task4/acc_census.csv`); gate re-run registered as a re-measurement of the same claim, same 0.15 px bound | [tool-gate-registration-2.md](tool-gate-registration-2.md) |
| 16 | 23 Aug | E3-a amendment (`89df56a`): the first E3 pass "returned recovery errors equal to the applied displacement for **every** candidate at every magnitude, i.e. nothing was recovered and no ranking existed"; fix = winsize 64 + 5/10/20 m displacements, "**No other parameter changes**"; commit message: "registered … BEFORE any ranking was read" | Three claims fail against the run artifacts (`tool_runs/E3/`). (1) Pass 1's sub-window cases (0.5/1/2.5/5 m at winsize 15) **had recovered** (0.5 m: 0.438/0.523/0.994) and their ordering was written to `E3_summary.json` **69 s before** the amendment commit; the session observation log records the ranking as read one minute before the commit. (2) The winsize-64 re-run then failed at every magnitude — the registered fix did not work. (3) The reported "working pass" is a **third, unregistered configuration**: winsize back to 15, displacements 0.5–5 m — contradicting "no other parameter changes" on both counts; its 0.5/1/5 m cells are pass-1 cells (bit-identical rows in `E3b_recovery.csv`), only 2/3 m were newly run, after two failures were known | E3 reclassified **exploratory** (consistency check on direction, not confirmatory) in [positioning-results.md](positioning-results.md); AMENDMENT E3-b added to the registrations, explicitly labelled retrospective; bootstrap CIs registered as a precondition for any cross-candidate claim, with the overlap interpretation rule stated **before** computing; the C1 operational-resolution run deferred until after that gate | [positioning-registrations.md](positioning-registrations.md), [positioning-results.md](positioning-results.md), [E3-session-log-excerpt.md](E3-session-log-excerpt.md), `tool_runs/E3/` |
| 17 | 23 Aug | T1 registration: "Statistic and matchers … **secondary matcher ORB+RANSAC** (B3 harness)" — a registered element of the protocol | The secondary matcher was **never run**: no ORB artifact exists for either T1 extent, and [T1-benchmark-results.md](T1-benchmark-results.md) reported KLT-only results without disclosing the omission. Found by the registration audit ([T1-audit.md](T1-audit.md)) — the same audit T1 otherwise passes in full: timeline claim true (only input rasters predate the amendment), all 70 reported cells reproduce from the raw CSV, every other registered parameter matches the run configs | omission disclosed in the results document; audit committed; running the ORB half remains open work ([paper-roadmap.md](paper-roadmap.md)) — until then T1 is quotable as a primary-matcher result with this caveat, not as "protocol executed in full" | [T1-audit.md](T1-audit.md), [delivery-registrations.md](delivery-registrations.md) §T1 |
| 20 | 24 Aug | B3 part 2 results: "the C2−C1 KLT gap conditioned on photometric + gradient similarity **loses 0% of its magnitude** (ank130 −0.702 → −0.702; eu150 −0.434 → −0.434) … **the 'trained-on-the-metric' explanation receives no support**" | The reported conditional is the **OLS fitted value evaluated at the covariate means**, which is algebraically identical to the raw mean for *any* covariates. "0% lost" is a property of the estimator, not a measurement — the test as run could not have returned anything else, and cannot detect mediation of any size. (Signature visible in the run's own log: photometric-only, gradient-only and both-covariate variants all return 0%.) Model reconstructed from the surviving grays; both reported means **and both reported SEs** recovered to three decimals, fixing the model. The mediation-capable statistic from the same fit — the gap at Δsimilarity = 0 — gives ank130 **−0.395 ± 0.124** (43.8% lost, still significant, t = −3.19) and eu150 **−0.106 ± 0.088** (75.6% lost, **significance lost**, t = −1.20); gradient similarity carries it, photometric similarity is inert | **the "no support" sentence is withdrawn.** Registered "fully mediated" (≥80% AND loss of significance, jointly) is still not met on either set, so the withdrawal condition for "matcher-independent" does **not** fire — but the registration's own fallback ("partial mediation … wording narrowed proportionally") now governs. Disclosure line in the results document; the narrowed wording is drafted with the manuscript, not retrofitted here | [B2-B3-audit.md](B2-B3-audit.md) §B, §C |
| 21 | 24 Aug | B3 part 1 registration: "global MI shift per chip (grid search ±8 px, 64-bin joint histogram, **parabola subpixel**)" | **The subpixel refinement was never applied.** All 930 MI values lie exactly on the integer pixel lattice — every one equals hypot(i, j) for integers i, j ∈ [−8, 8] to 1e-7, which a parabola fit on an MI surface would essentially never produce. The ±8 px grid *is* confirmed from the data (max = hypot(8,8) exactly), and that bound censors 147/930 measurements (15.8%) at radius ≥ 8 px, 39 pegged at the corner. Entry-17 class: a registered element with no artifact and no disclosure | disclosed in the results document; MI is quotable as an **integer-pixel, bound-censored** statistic. Censoring is heavier for the worse arms (ank130 at ≥8 px: pretrained 27.7%, C1 17.7%, C2 8.5%), so the reported −1.260 ± 0.261 is a **lower** bound on the MI margin — a bias against our own prediction, which may stand disclosed. Re-running MI with refinement is open work | [B2-B3-audit.md](B2-B3-audit.md) §C |
| 26 | 24 Aug | C4's registered stop rule — "C1's with the term renamed: if G degrades early despite the warm-up (**G_LPIPS rising over the first two main-stage epochs**), the run stops and reports its curves, no rescue by improvisation" | **The condition was met on its literal reading and the run continued to epoch 20.** C4's per-epoch G_LPIPS means: warm-up 56.24, 55.48; main stage **54.37 → 54.65 → 55.02** — rising at each of the first two main-stage epochs. **The rule is mis-specified.** *(Argument revised 2026-08-24, second pass — the first version rested on the two-epoch window; that window is confounded and the revision says so. Nothing disclosed is withdrawn.)* **The window cannot carry the argument**: warm-up presence is perfectly collinear with discriminator presence — C1 and C4 carry the 2-epoch warm-up at 2e-5, C2 and C5 do not — so the first two main-stage transitions are exactly where the learning rate jumps **2e-5 → 1e-4, a 5× increase**, in precisely the two arms that rise and neither of the two that do not. It cannot separate "the adversarial term competes with the reconstruction term" from "a 5× LR jump causes a transient bump". The two adversarial arms do not even behave alike in it: **only C4 rises at both transitions; C1 rises then falls** (33.582→**34.224**→33.858). **The evidence is the sustained main-stage trend**, over all 18 main-stage epochs, after any LR transient has had time to decay: neither adversarial arm reduces its reconstruction loss (C1 **+1.16%**, slope −0.001/epoch — flat, no trend; C4 **+2.50%**, slope +0.056/epoch — rising), while both non-adversarial arms reduce it by roughly 8% (C2 **−7.90%**, C5 **−7.54%**). **The transient explanation is refuted for C4 and not for C1, and they are written differently**: C4's main-stage start of 54.374 **is the run minimum — 0 of 18 main-stage epochs fall below it**, so across twenty epochs it never returns below where it began, which an LR-jump transient would do within a few epochs; **C1 does recover below its start**, first at epoch 7 (33.536) and deepest at **epoch 16 (33.118, −0.46 below the start)**, so for C1 the claim is only that the reconstruction loss fails to fall, not that it rises. **A gate that treats "reconstruction loss not falling" as a divergence symptom fires precisely when the treatment under test is working.** Under the rule as written C1 — the arm this package replicates — should also have been stopped, in a package registered five days earlier; the coarse half has never been applied literally, and it carries no threshold | **AMENDMENT C45-a**: the coarse half is re-registered, RETROSPECTIVELY and labelled as such in the E3-b manner, with the original preserved verbatim and recorded **FAILED** — new rule: the run stops if the per-epoch reconstruction loss rises >10% above the lowest value seen so far in the main stage, sustained two consecutive epochs; the sharp cold-D spike half is unchanged. **C45-a binds FUTURE runs only and does not retroactively bless the completed arms** — its threshold was chosen with all four completed arms' values already known and all four pass it, so it is a calibrated rule, not a blind pre-specification, and a gate no arm triggers whose threshold was set after seeing the arms is what **standing practice 4** forbids ("failed gates reported, never adjusted"); only that practice's second half ("mis-specified gates re-registered with the original preserved") licenses the amendment at all. The calibration values are printed in the amendment (largest running excursion C1 +3.66%, C4 +2.50%, C2 +3.14%, C5 +0.73%); its first genuine test is the next set of runs. **The completed seed-42 arms stand on the ORIGINAL rule, which fired and was not acted on**, defended by the mis-specification argument and the counterfactual below. **The counterfactual makes the conclusion independent of the decision**: had C4 stopped at epoch 5 the primary band still fires — C4 − C5 = +0.441 ± 0.039 (11.3 SE) at epoch 5 and +0.254 ± 0.040 (6.4 SE) at epoch 2, ≥ 6 SE at every epoch. **That the results document then reported the rule as "not triggered" is a separate failure and is entry 27; this entry does not cover it** | [phase-c-lpips-registration.md](phase-c-lpips-registration.md) AMENDMENT C45-a, [phase-c-audit.md](phase-c-audit.md) §C.2, §C.4 |

Entry 5 is the only one so far where the run was corrected rather than the record. What forced
the restart was not magnitude (1.49% of C1's step budget, at the schedule's lowest rate, in a
stage whose purpose the D_real trace shows was already met inside epoch 1) but **direction**: the
initial let-it-run decision was made on the symmetry claim, and when that claim fell, the
criterion had to be re-applied rather than defended. The evidence that the warm-up's purpose was
met — D_real 0.307→0.156 across epoch 1, G_L1 flat at ~32-34, zero spike events against a 20-row
running median — is an empirical claim that the risk did not materialise, not a structural
guarantee, and the two must not be conflated when deciding whether a bias can stand.

**Entry 26 — addendum, 26 Aug, applied 13 Sep.** The warm-up de-confound
([warmup-deconfound-results.md](warmup-deconfound-results.md) §4) gave two non-adversarial
arms C1's exact warm-up ladder at seed 43. Neither rose: the second registered branch fired,
so the learning-rate-jump explanation of the two-epoch window is refuted at that seed, and
entry 26's revision was more conservative than it needed to be. The paragraph the
registration promised: *no arm without a discriminator has ever shown the window rise,
including now that two of them have been given the warm-up that was the competing
explanation; but not every arm with a discriminator shows it either, and which one does
varies by seed. The rise does not come from the learning-rate jump and has only ever
occurred in the presence of a discriminator, but it is not a reliable per-arm signal, and a
stop rule keyed to it would still fire inconsistently across seeds.* Entry 26's decision to
rest the argument on the sustained trend is therefore retained on its merits. The secondary
expectation ("warm-up does not touch the sustained trend") was **not cleanly met**: the
warmed variants fall by roughly 60% of their un-warmed counterparts' proportional fall
(−5.32% vs −8.00%; −2.98% vs −5.16%), the sign and the ordering against the discriminator
arms survive, the magnitude is attenuated. Nothing is withdrawn. n = 1 seed, a mechanism
probe; it enters no contrast.

**Decision reversals are recorded here too, because a reader of the log should see them beside the corrections they resemble.**

| # | date | what was decided | why it was reversed | resolution | where |
|---|---|---|---|---|---|
| 41 | 26 Aug / 13 Sep | **Supervising session's decision, reversed by the supervising session.** On 26 August the structural decision cut the 1/256 geometric-error finding from the letter for space (letter-skeleton.md binding sentence 13 amended to an arXiv-only rule), leaving Section II-F with a one-clause disclosure that deferred the finding to "the extended version" | The arXiv version has no page limit, and by 13 September the arXiv-length draft *was* the extended version, so the deferral in II-F pointed at itself and the finding appeared in no document a reader could reach | Reversed 13 September by the supervising session: the finding restored to II-F from the 26 August draft text (cadad66, block F), numbers re-checked against `geometry-finding.md` and `paper-context-addendum.md` §19, the deferral sentence deleted, the variance qualifier in the same paragraph as required. Recorded as a decision reversed, not as a correction of the drafting session | [letter-skeleton.md](letter-skeleton.md) §4 item 13; the letter's `EVIDENCE.md` |

| 43 | 13 Sep | **Practice-adoption test, recorded as required by the supervising session.** Five GenCP `CLAUDE.md` practices (5, 9, 10, 11, 12) had no canonical number in `standing-practices.md`. Test applied, from practice 11: a practice is adopted only if it states the **class** it governs rather than the instance that prompted it | **Passed, adopted as canonical 13–16:** CLAUDE.md 9 (seed and numerics-affecting versions recorded per run — class: a run's numerical inputs are recorded with the run); 10 (verifiers run against known-true, known-false and degenerate invocations — class: a verdict is trusted only after the vacuous-verdict cases are run); 11 (a check is born with a failing case — class: order of construction); 12 (an assumed unit is checked where assumed — class: enforcement at the point of assumption). **Failed, not adopted, not deleted:** CLAUDE.md 5, "Registration text must name the exact corpus and the exact reference directory." The sentence that fails it is its own first sentence: *corpus* and *reference directory* are the two inputs that went wrong in Gate D, not the class of inputs a registration depends on; the class rule would be "a registration names every input it depends on by an identifier that resolves, so that a wrong name is detectable by reading". It may be rewritten in that form and reproposed | `standing-practices.md` 13–16 and its numbering note; the six "practice 9" citations annotated on 13 Sep now resolve to canonical 13 through the mapping | [standing-practices.md](standing-practices.md) |

### Registration-versus-implementation mismatches — one class, three instances (entries 36–38, applied 13 Sep per standing practice 11)

Each was found after the fact and is disclosed where it was found and **not repaired**: a
rule rewritten after seeing which way it cuts is indistinguishable from a rule adjusted to
pass. In each the reading held either way. Numbered 36–38 on application; the three had no
drafted numbers.

| # | date | what was claimed/registered | what was actually true | resolution | where |
|---|---|---|---|---|---|
| 36 | 26 Aug | Warm-up de-confound branch text: "IF C2_warmup and/or C5_warmup RISE **as C1 and C4 did**" — presuming both discriminator arms rise at the first main-stage transition | True at seed 42 (C1 +0.642, C4 +0.276); false at seed 43, where C1 rises (+0.141) and C4 falls (−0.454). The clause had no determinate referent | The branch fired on its antecedent (neither warmed arm rose), so nothing changed; the qualification is recorded beside the result | [warmup-deconfound-results.md](warmup-deconfound-results.md) §4 |
| 37 | 25 Aug | Hardware gate acceptance rule: "pool only if EVERY Modal-vs-Kaggle difference at seed 43 is smaller than the corresponding s43-to-s44 spread" — a single global verdict | Each quantity is scaled to its own spread, so the most reproducibly-measured quantity governs the package: edge_C1's spread is 0.0042, an order of magnitude tighter than the positional quantities', and one quantity vetoed ten. Noticed only after seeing which quantity failed | NOT POOLED stands as the registered branch selected; the flaw is recorded, not repaired; blocks analysed separately throughout | [hardware-gate-results.md](hardware-gate-results.md) |
| 38 | 25 Aug | AMENDMENT SEED-c (d): "C5's edge mean the highest of **the four arms**" | `seed_analysis.py:212` implements the tie rule over `("pre", "C1", "C2", "C4")` — five arms, pretrained included. The harness was stricter than the registration (the safe direction) | Reading held either way, 6/6; registration left as written with the mismatch disclosed | [phase-d-closeout.md](phase-d-closeout.md) §C, [standing-practices.md](standing-practices.md) 11 |

## Tier 2 — unrecoverable artifacts

Each entry states which reported numbers can no longer be checked against anything.

| # | date | what was claimed/registered | what was actually true | resolution | where |
|---|---|---|---|---|---|
| 10 | 19 Aug | (record damage, disclosed) Phase B's `test_opt.txt` options record assumed safe during inference replication | `test.py` writes its options file into the checkpoints directory; the replication run overwrote the untracked original | disclosed; the original run's options survive in prose in geometry-finding.md §"saved options file"  **Uncheckable as a result:** the Phase B replication run's own options record — every `--flag` actually passed to that inference, including the dropout-relevant ones. The reported Phase B numbers stand on their own artifacts; what cannot be re-derived is the exact invocation that produced them, which survives only as prose. | this file |
| 22 | 24 Aug | Standing assumption that a results table can be re-derived from its run configuration (entry 16's own lesson: "citing, in every results table, the config file of the runs it came from") | **B3's harness is gone.** `tubitak/data/tool_runs/B3/B3_run.py` — named in the contemporaneous log as the script that produced every B3 number — no longer exists, and B3 wrote no config beside its outputs; parts 2 and 3 wrote **no per-chip artifact at all**. Consequence: ORB `nfeatures 2000` and the `estimateAffinePartial2D` 3 px threshold are attested only by the session log, and the **BFMatcher Hamming cross-check and the 64-bin joint histogram are attested nowhere**. Separately, B2's `B2_infer.sh`, `B2_karios.py` and `B2_mean_warp.py` were **overwritten in place on 24 Aug 02:52–02:53** by the C4/C5 package — the entry-10 pattern recurring | recorded, not repaired. B2's numbers survive intact because the audit reproduced all 336 cells from the raw KARIOS CSVs without touching the mutated scripts, and `B2_score.py` (the script that made the table) is untouched. The practice that fixes this class was adopted two days later for C4/C5 ("harness preserved in git", `40cde9b`) and must be applied retroactively: commit B2's and any future package's harness beside the results  **Uncheckable as a result:** ORB `nfeatures 2000`, the `estimateAffinePartial2D` 3 px threshold, the BFMatcher Hamming cross-check and the 64-bin MI joint histogram — i.e. four of the registered parameters behind every B3 part-1 number; and B3 part 2's covariates and part 3's per-chip ratios, so the mediation and edge-ratio distributions cannot be rebuilt at their original definitions. The part-1 *scores* remain checkable (`B3_scores.csv` survives and the audit reproduced every cell from it); the *parameters that produced them* do not. | [B2-B3-audit.md](B2-B3-audit.md) §C-4 |
| 23 | 24 Aug | That the paper handoff document's **§16 (prior art) and §22 (mechanism caveat)** were part of the versioned record and could be cited as such — the `paper-roadmap.md` amendment (`c809ee8`) linked to them by filename as if they were | The file `tubitak/docs/paper-context-addendum.md` was **untracked**. Its binding manuscript caveats therefore had **no commit timestamp** and could not be cited as predating anything, and the links broke for anyone outside this machine. The repository copy had **additionally gone stale**: it was a §14–23 extract whose §21 still read "B2, B3 — Not yet audited", whose §16 predated the related-work pass, and which was superseded by a revision that **withdraws the B3 mediation result** (§5, per entry 20). A `related-work.md` referenced as done did not exist in the repository at all. Same preservation class as entries 10, 13 and 22 — in document form rather than artifact form | both files committed here, byte-identical to the provided revisions, under the repository filename the amendment already links to; §14–23 diffed section by section against the incoming file **before** overwriting (superset confirmed: no section, edit or number present in the untracked copy is absent from the replacement — the only repo-only text was the extract's own `# ADDENDUM` header, which the full document obsoletes). All four links now resolve  **Uncheckable as a result:** nothing numeric — the loss was of a commit timestamp, not of data. What could not be established is that §16's prior-art record and §22's mechanism caveat predated anything, so neither may be cited as having been fixed before the results they qualify. | [paper-context-addendum.md](paper-context-addendum.md), [related-work.md](related-work.md), [paper-roadmap.md](paper-roadmap.md) |
| 25 | 24 Aug | That [phase-c-results.md](phase-c-results.md)'s C1/C2 table rests on artifacts that can be re-examined — and, downstream, that the Gate-1 comparison target the whole C4/C5 registration is scored against (**C2 − C1 = −0.638 ± 0.054 px**) is a checkable number | **No per-chip C1 or C2 artifact from the 2026-08-19/20 phase-C scoring run survives.** The earliest directory under `tool_runs/` postdates it (2026-08-20 19:32); `ankara/run/` holds only the pretrained arm's outputs; `turkey_karios.csv` has a pretrained column and no other. The pretrained row **does** reproduce (10/12 cells exact). Everything C1/C2 — ten per-stratum values, both ALL values, all three paired deltas, and R2's rho +0.232 / +0.032 — has no raw layer. Entry-22 pattern, landing this time on the number the letter's primary comparison is registered against | recorded, not repaired. The Aug-21 seed-42 redraw (`B1_per_chip.csv`) corroborates every cell within measured dropout redraw noise and changes no verdict or sign (C1 median 1.869→1.7939, C2 0.929→0.9744, C2−C1 −0.638→−0.6995), and [phase-c-lpips-results.md](phase-c-lpips-results.md) **already publishes both values side by side**. Corroboration by a different draw is not recomputation: phase-c is quotable as pre-registered with this caveat, and the letter should carry the reproducible same-draw-family value beside the committed one  **Uncheckable as a result, stated exhaustively:** phase-c-results.md's C1 and C2 columns in full — Q1–Q5 and ALL median residual and point count for both arms (22 cells), the paired C1 − pretrained −0.530 ± 0.070, C2 − pretrained −1.167 ± 0.074 and **C2 − C1 −0.638 ± 0.054**, R1's paired median 0.524 and point-ratio 1.29, R2's rho +0.232 and +0.032 with their CIs and Q1 improvements, and R4's Q5 figures. The pretrained column is unaffected and reproduces. | [phase-c-audit.md](phase-c-audit.md) §B.2 |
| 32 | 26 Aug | [phase-c-europe-results.md](phase-c-europe-results.md) §63–64 and [gcp-veto-rule-results.md](gcp-veto-rule-results.md) §29: the per-chip artifacts are declared "session scratchpad; **regenerable end-to-end from committed scripts and registrations**" | **The artifacts are gone and the scripts were never committed, so the regenerability claim that justified not committing them is false.** `eu_per_chip.csv`, `blur_control_per_chip.csv`, `eu_decomposition_per_chip.csv`, `veto_features.csv` and `veto_rule.py` return zero hits repository-wide; `git ls-files` finds no committed script for blur, sigma fitting, spectral matching, decomposition, bootstrap, FFT periodicity, point-count floors or the veto rule. Six of seven Phase D checks and the veto rule therefore have **no artifact of any kind** and leg C of the audit could not be run for them. The registrations describe the procedures well enough to re-implement, but re-implementation produces new numbers, not the published ones. Same class as entry 22 (B3's harness deleted), recurring after that lesson, and worse in one respect: entry 22 cost four parameters, this costs the numbers. Affects Table II's blur row (σ = 0.45, −6.1%/+1.7%) and corrected-georeferencing row (~86% scatter) in full | Re-run both procedures from their registrations, commit the per-chip outputs **and the scripts**, and verify the published values; until then neither row enters the letter without an explicit unreproducible-provenance caveat. Standing fix: `tubitak/data/*` is gitignored, so no `tool_runs/` artifact in this project is protected — analysis outputs that support a published number are committed outside `data/`, as the seed-block loss logs now are | [phase-d-audit.md](phase-d-audit.md) §C |
| 33 | 26 Aug | [phase-d-checks-registration.md](phase-d-checks-registration.md) check 5 ("Europe, **Ankara**, Cappadocia, Tuz Gölü, and the salt/non-salt splits" recomputed under floors 0/10/20/30) and the ratio addendum's requirement that per-stratum gains be "reported alongside R **so the aggregation hides nothing**" | **Two registered reporting requirements were not met.** No floor-sensitivity line for **Ankara** exists in any document; floors are reported for Europe, Cappadocia, Tuz and the salt splits only. Only the aggregate ratio (1.188/1.258 = 0.945) is published; the per-stratum gains are absent, defeating the stated purpose of the requirement. With no artifact surviving (entry 32) the audit **cannot determine whether the Ankara sweep ran and went unreported or never ran** — the entry-19 shape in its unresolvable form. Additionally, check 7b's registered conditional ("if the improvement is mostly scatter … restraint/blur carries the burden") was triggered (~86% scatter) but never explicitly discharged, so the burden transferred onto check 3 — the least verifiable result in the package — without being stated | Both items reported when the procedures are re-run per entry 32; the burden-transfer sentence added to the restraint verdict | [phase-d-audit.md](phase-d-audit.md) §B-2, §B-3 |

## Tier 3 — reporting errors

The runs were right; the writing was not.

| # | date | what was claimed/registered | what was actually true | resolution | where |
|---|---|---|---|---|---|
| 1 | 18 Aug | "We are 3× worse than upstream on KARIOS" | Compared two different quantities; on upstream's own statistic we are 4.5× better | claim withdrawn | [README](../README.md), [karios-validation.md](karios-validation.md) |
| 18 | 23 Aug | phase-c-config.md arm table: "C1 — GAN + L1 (repo defaults: **lsgan**, λ_L1 = 100)" | The fork's pix2pix defaults override `--gan_mode` to **vanilla** (BCE-with-logits): `train_options.py` defaults lsgan, but `Pix2PixModel.modify_commandline_options` sets `gan_mode='vanilla'`, and the C1 run log records `gan_mode: vanilla`. The runs were correct; the parenthetical was wrong | doc error recorded, not silently edited; found during Gate 0 of the C4/C5 package while checking the BCE bridge to the published paper (Table 5: Discriminator Loss BCE) — the correction *strengthens* comparability with the published objective | [phase-c-lpips-registration.md](phase-c-lpips-registration.md), [phase-c-config.md](phase-c-config.md) |
| 19 | 24 Aug | B2 registration: "score **RGB KLT and BT.601-gray KLT** against the standard references"; B3 part 3 registration: "the registered read-out is the per-arm ratio **distribution**" | Both halves of B2 **did run** (240 KARIOS runs; the full RGB table sits in `B2_summary.json`) — but [headline-results.md](headline-results.md) reports BT.601 only and never says the RGB half is absent from it. B3 part 3's distribution was computed at quartile resolution and **three medians** were carried into the results document. Not the entry-17 pattern (nothing went unrun); a registered *reporting form* narrowed at writing time without disclosure. The omission runs against our own claims: on RGB, C2 − pretrained widens to −1.028 ± 0.188 px and C2 − C1 to −0.258 ± 0.090 px, and the restatement check is also inside the band (\|0.6030 − 0.591\| = 0.012) | RGB table and both distribution layers published in the audit; disclosure line added to the results document in the same commit. B2 remains quotable as registered and executed in full | [B2-B3-audit.md](B2-B3-audit.md) §C-1, §B |
| 24 | 24 Aug | [phase-c-lpips-results.md](phase-c-lpips-results.md) secondary table: "pretrained 1.370 **± 0.108**, C1 0.764 **± 0.043**, C2 0.593 ± 0.041, C3 0.611 **± 0.043**, C4 0.844 **± 0.054**, C5 0.663 **± 0.045**"; plus the ank130 C4 mean "1.966" and the C1 edge-ratio mean "1.097" | **Five of the six SE values do not come from the artifact and cannot be reproduced by any estimator.** `C45_b2_summary.json` stores 0.1612 / 0.0712 / 0.0409 / 0.0375 / 0.0949 / 0.0660 — exactly `sd/√20`, and exactly what B2 committed for the four pre-existing arms. Only C2's 0.041 matches. The printed five are not the SE of the mean, the SE of the median, a 4,000-resample bootstrap median SE, a MAD- or IQR-based SE, nor the RGB-band SEs, and they appear nowhere else in the repository. Separately, two of twenty reported 3-decimal values are **double-rounded** from the artifact's 4-decimal field: C4 ank130 mean is 1.965452 (correct 1.965, printed 1.966) and C1's edge-ratio mean is 1.096463 (correct 1.096, printed 1.097). Third, **half-integer point-count medians are printed as integers with no note**, six cells across both phase-C documents (C4 ank130 61.5→62; secondary C1 163.5→164, C4 155.5→156, C5 224.5→**224**; phase-c-results pretrained Q2 37.5→38, Q3 51.5→52) — banker's rounding, which is why one goes down while two go up | recorded, not silently repaired. No registered band depends on any of them: the secondary row carries no registered band by design, C4's mean is not a band quantity, and C1's edge ratio clears "near 1.0 ≥ 0.8" at either value. **Resolved 2026-08-24: the ± column was removed from the table entirely** rather than reprinted — five of its six values have no known origin, and the paired deltas below the table already carry the uncertainty for every comparison the row supports. The removal and its reason are stated in the document. The two double-rounded values were corrected at every occurrence across all documents (1.966→**1.965**, 1.097→**1.096**); the half-integer point counts were enumerated and left as they are, pending a decision on how to state them. Everything else in that table — means, medians, point counts, all paired deltas, the ordering — reproduces exactly | [phase-c-audit.md](phase-c-audit.md) §B.1 |
| 27 | 24 Aug | [phase-c-lpips-results.md](phase-c-lpips-results.md) training-run table: "stop rule — **not triggered**: G_LPIPS at main-stage start (54.4) is the run minimum, below warm-up (56.2) — no cold-D spike" | **The rule had fired** (entry 26). The document reported it as untriggered by citing the **sharp** half of C1's two-part rule ([phase-c-config.md](phase-c-config.md): coarse half = "L1 rising over the first two main-stage epochs"; sharp half = a generator-loss spike in the first few hundred iterations, the cold-D signature) **in place of the coarse half the registration names**. Both cited facts are true — 54.37 is the run minimum and it is below the warm-up's 56.24 — and neither is the registered test. **The substitution was not disclosed as a substitution**: a reader of that cell learns that a registered check passed, when what happened is that a different check was run instead | **a reporting error, not a protocol deviation, and the mis-specification defence in entry 26 does not touch it.** The cell now reads "**FIRED, and was not acted on**", names the coarse rule and the three epoch means that fired it, states that the earlier wording came from citing the spike test, and points to entry 26 for the separate question of why the run was allowed to stand. Entry 26 defends the decision; this entry records that the record misstated it | [phase-c-lpips-results.md](phase-c-lpips-results.md), [phase-c-audit.md](phase-c-audit.md) §C.2 |
| 28 | 24 Aug | The **sharp half** of the cold-start stop rule, described across two packages as the operative divergence test: [phase-c-config.md](phase-c-config.md) calls it "a generator-loss spike in the first few hundred iterations, the actual cold-D signature" and sets `--print_freq 10` specifically so it can be seen; AMENDMENT C45-a re-registered the coarse half and said the sharp half "is unchanged and remains the operative divergence test" | **It had no threshold in any document, no implementation anywhere in the repository, and never fired because nothing was watching.** phase-c-config.md defines it in words with no magnitude; corrections-log entry 5 supplies the only quantified trace — "zero spike events against a 20-row running median" — which fixes a **baseline** but not a **threshold**; and there is no spike-detection code in `tubitak/kaggle/train_c1_c2.py`, in `tubitak/scripts/`, or anywhere else. For C1/C2 (19 Aug) and C4/C5 (23 Aug) the record therefore claimed an active divergence test that could not have detected anything, and "zero spike events" in phase-c3-results.md was a statement about a check that was never executed. **This is not entries 26/27**: there a rule fired and was not acted on; here a rule could not fire at all | caught while writing [seed-replication-registration.md](seed-replication-registration.md) and trying to quote the original threshold — there was nothing to quote. Resolution: the rule is **calibrated on the four completed seed-42 logs** (max ratio of a reconstruction-loss row to its trailing 20-row median, first 500 optimizer steps: C1 1.5692, C2 1.5091, C4 1.1626, C5 1.0906; highest anywhere 1.8792), **threshold set to 2.5** — 1.59× the highest windowed value, 1.33× the highest seen anywhere — and **implemented** in `run_train()`, which evaluates after epoch 1 of every stage and terminates the run. Labelled newly specified rather than quoted, calibrated on completed runs (disclosed), prospective only, and **a novelty detector rather than a validated divergence test**: it has never been shown to catch divergence, because divergence has never been observed here | [seed-replication-registration.md](seed-replication-registration.md) §Stop rule, [train_c1_c2.py](../kaggle/train_c1_c2.py) |
| 29 | 25 Aug | The seed-replication registration's invariance table listed **"Kaggle T4, same image, same torchmetrics 1.9.0"** as a held invariance, and standing practice 1 requires every gate registration to state what it assumes identical on both sides | **The image contents were never captured by any run, so the claim was unfalsifiable.** The training script logged exactly two versions — torch and torchmetrics — and `requirements.txt` carries floors (`torchvision>=0.5.0`) not pins. Nothing recorded torchvision, Pillow, numpy, the Python version or the image digest, for **any** run in the package: seed 42's C1/C2 (19 Aug), its C4/C5 and seeds 43/44 (23–24 Aug). "Same image" was therefore an assumption *within* the Kaggle set as much as across the later hardware move — a gate stating an invariance it had no evidence for, which is precisely what standing practice 1 exists to prevent. It is not a formality: `RandomCrop` and `RandomHorizontalFlip` consume the seeded torch RNG (a torchvision change to draw counts diverges the augmentation stream) and `Resize(BICUBIC)` delegates to PIL (so pixel values depend on Pillow) | caught while trying to pin the Modal image against the Kaggle one for AMENDMENT SEED-b — the pin had nothing to pin *to*. Versions **recovered** 2026-08-25 by a probe kernel and labelled recovery, not capture (it ran ~20 h after the seed-43/44 runs and cannot prove the image is unchanged; the two logged values match and the image BUILD_DATE 2026-06-29 predates every run, which is evidence). Recovered: Python 3.12.13, torch 2.10.0+cu128 (CUDA 12.8, cudnn 91002), torchvision 0.25.0+cu128, torchmetrics 1.9.0, numpy 2.0.2, Pillow 11.3.0, scipy 1.16.3, digest `gcr.io/kaggle-gpu-images/python@sha256:37c64f7d…`. **SECOND INSTANCE, 25 Aug — enumeration order.** The same failure, found the same way. `data/image_folder.py:make_dataset()` sorts the walk tuples but not `fnames`, so per-directory file order is whatever the filesystem returns, and **no run ever recorded it**. Measured while pinning the Modal data path: the network Volume enumerates sorted (`4b5f2320…`), a container-local copy does not (`a4171d88…`), 5,577 files both ways with byte-identical contents. Staging to local disk would therefore have changed which files the seeded shuffle maps to, silently, while every content hash still matched. Kaggle's order was never captured and cannot be recovered. **FOURTH AND FIFTH INSTANCES, 25 Aug — evaluation hardware and the LPIPS backbone weights.** Evaluation has run on the same local machine for every seed (42, 43, 44) and that was never asserted as an invariance; it is now, and evaluation must not move to Modal. And `vgg16-397923af.pth`, the VGG-16 that torchmetrics' LPIPS uses as its feature extractor, was **downloaded at run time on both platforms** — it is part of C4/C5's objective function, so a difference would mean those arms trained against a different loss with nothing reporting it. Now baked into the Modal image with sha256 `397923af8e79cdbb6a7127f12361acd7a2f83e06b05044ddf496e83de57a5bf0` pinned and asserted at preflight. All six Kaggle LPIPS logs show the same download URL, and torchvision's check verified the filename's 8-hex prefix — but no Kaggle run recorded the full hash, so the Kaggle-side file is **strongly evidenced, not verified**, and is recorded as an unverifiable axis. **THIRD INSTANCE, same day — the staging tar itself.** The first tar was built on macOS, where libarchive stores an xattr header per entry; GNU tar on Linux materialises each as an AppleDouble `._name` file, and `is_image_file()` accepted them because they still end in `.tif`. The staged training set came to **n = 11,154 — exactly double** — half of it 4 KB metadata. Caught at preflight by the ordered-list capture added hours earlier, before any training step ran. **The published Kaggle runs are NOT affected:** every seed-42, 43 and 44 log carries `[preflight] training pairs: 5577`, matching the source tree, and the corruption existed only inside a tar built later for the Modal move. **What made it invisible is worth stating: every content hash matched throughout, including the pretrained `5938…a022`, because the real files were all present and correct — the corruption was ADDITIVE.** No content check could have seen it; only a count or an order check could, which is exactly why the ordered-list hash went into preflight. Fixed with `--no-xattrs --no-mac-metadata` (`COPYFILE_DISABLE=1` does not govern libarchive xattr storage and did not help). Resolved by a **committed** patch (`tubitak/modal/patches/image_folder_sorted.patch`, applied with `git apply`, never sed'd into a clone) that **restores the Volume's own sorted order on local disk rather than imposing a new one**; the resulting `image_folder.py` sha256 and the ordered file-list hash are both logged at preflight, and the **hash is asserted against its expected value**, not merely printed — one check covering both a count change and a same-count change to contents or order. The count assertion is kept alongside it because when it is the count that broke, it gives the clearer message. The residual — whether Kaggle matched — is **measured rather than argued**: C2 runs twice in the gate, sorted and unsorted, at fixed hardware and seed. **SIXTH INSTANCE, 25 Aug — the training-script version inside the containers, and this one is a NEAR-MISS, not a catch.** Until 19:51 every container cloned `-b tubitak-tr` and took whatever HEAD was at container start: the code version was an axis nothing pinned. At 19:16, while C5 was still training, commit 96503b7 (a code-review pass) changed `tubitak/kaggle/train_c1_c2.py` — sha256 `839e1aad…`, the file all four completed seed-43 Modal arms ran (C1/C2 at 4817b90, C4/C5 at f2dc962), became `878fa200…`. C5 completed at 19:50; C2_unsorted had not yet been launched. Had it launched in that 35-minute window, a bare branch clone would have given it a different training script from its four siblings, and nothing would have reported the difference. The checkout pin (80206b4, 19:51) was committed AFTER the exposure existed, not before — it closed a window this package was lucky with, and is recorded as luck rather than foresight. Decision for the fifth arm, recorded before its launch: C2_unsorted runs at the pinned f2dc962 like its siblings — every arm of the replication runs ONE commit, which is simpler and stricter than proving equivalence for each later change. 96503b7's substantive fixes (build_reference_set's un-disableable flag, acceptance_test's silently dropped chips, receptive_field_check's unreachable returns) are all outside the training path, and its evaluation-side edits passed the seed-42 reproduction gate re-run at HEAD the same evening (every column max\|diff\| 0.0, `C45_per_chip.csv` byte-identical to the original gate run's `be1360cd…`). If a training-path fix ever becomes necessary, it gets its own equivalence run against a completed arm before adoption. | **Prevention, generalised across all instances:** every property the invariance table asserts must have a corresponding **preflight capture**, or the assertion is unfalsifiable and the gate is stating something it cannot be held to. `pip freeze` (image version) and the ordered file-list hash (enumeration order) are its first two implementations; `log_environment()` in [train_c1_c2.py](../kaggle/train_c1_c2.py) is where they live, on Kaggle and Modal alike; the container checkout pin is the third (`GIT_COMMIT` in [gencp_modal.py](../modal/gencp_modal.py), the resulting HEAD logged at preflight): **while a registered package is executing, the branch it runs from does not move, and every container pins an explicit commit rather than tracking HEAD** | [seed-replication-registration.md](seed-replication-registration.md) AMENDMENT SEED-b, [train_c1_c2.py](../kaggle/train_c1_c2.py), [gencp_modal.py](../modal/gencp_modal.py) |
| 30 | 26 Aug | [packageA-results.md](packageA-results.md): "48 of 49 condition cells rank C2 first or tie it with C3 within noise" and "the single exception (EU-150 urban, phase corr.)" | **The count is not reproducible and the exception count is wrong.** The harness emits no cell total; `pkgA_summary.json` has none. Enumerating the artifact gives **56** condition cells (4 sets × 2 scopes × 7 conditions), or 42 excluding the pre-declared no-verdict Ankara-30 urban cells, or 24 counting only ordering verdicts against the RGB\|KLT baseline. The only scheme yielding a denominator of 49 is 7 conditions × 7 groups, and under it the numerator is **47, not 48**. Separately, the artifact contains **two** cells where C1 ranks ahead of C2, not one: EU-150 urban `bt601\|phase` (C1 by 0.0246 ± 0.2080, reported as "−0.03 ± 0.21") and EU-150 urban `mean\|phase` (C1 by 0.0092 ± 0.2168, **unreported**). Both are far below the registered 2 SE threshold, so **no registered ordering change is claimed either way and the package's conclusion is unaffected** — but "48 of 49" is quoted verbatim in the letter's Table II, where a reviewer recomputing it will find neither the count nor the single exception. The unreported cell is in the registered **secondary** band, the same shape as entry 19 in milder form | Results document to read "**two exceptions, both at EU-150 urban under phase correlation, one per band, both far below 2 SE**" and drop the unreproducible denominator; letter Table II corrected with it. Every other number in the package reproduces from raw to within 0.0008 px | [packageA-audit.md](packageA-audit.md) §C-2, §C-3 |
| 31 | 26 Aug | [letter-skeleton.md](letter-skeleton.md) §3 Table II, matcher-independence row: "matchers from three families \| ORB −0.613 ± 0.135 (n = 29 intersection), AKAZE, MI −1.260 ± 0.261 (lower bound); ordering preserved in 48 of 49 cells" | **The row fuses two different registrations into one apparent test.** Package A's registered matchers are KLT, NCC template grid and phase correlation; it never ran ORB, AKAZE or MI, and no such number exists in any Package A artifact. The ORB/AKAZE/MI deltas are **B3's** descriptor-family results ([B2-B3-audit.md](B2-B3-audit.md), [headline-results.md](headline-results.md)); only the ordering count is Package A's. "Matchers from three families" is ambiguous between B3's three descriptor families and Package A's three matcher families. The three caveats the row carries (ORB's 29-chip paired intersection, MI's lower bound from the never-run subpixel refinement, the delta-versus-matched-count n) are all **B3's** and all verified as still true, but they qualify only B3's half | Row split, or both provenances named in the cell, with each half carrying its own n and caveats. **The verdict "refuted" is unaffected and is in fact better supported than the row claims** — two independent registrations rather than one | [packageA-audit.md](packageA-audit.md) §E |
| 34 | 26 Aug | [phase-d-results.md](phase-d-results.md):9 describes the registration as "verification checks 1–7, **run and reported** before this document was written" | **Two of the seven are not reported in it, or in either of the other two Phase D documents.** Checks 3 (blur) and 7b (systematic/scatter) appear only in [phase-c-europe-results.md](phase-c-europe-results.md); the words "blur", "sigma", "systematic", "scatter" and "decompos" occur nowhere in `phase-d-results.md`, `phase-d-ratio-addendum.md` or `gcp-veto-rule-results.md`. Nothing is concealed — both outcomes are published, in a document committed in the same commit — but a reader auditing checks 1–7 against the document that claims them finds five, and the two absent ones are precisely the two that feed Table II | Cross-references added from `phase-d-results.md` to the two outcomes; the "run and reported" clause amended to say where | [phase-d-audit.md](phase-d-audit.md) §B-1 |
| 35 | 26 Aug | [evidence/MANIFEST.md](evidence/MANIFEST.md) listed 260 raster files under `input_render_warped/` and `real_chip_bt601/`, each with a sha256 and a byte size, and [evidence/rasters/README.md](evidence/rasters/README.md) stated they were "**committed 2026-08-26 as insurance**" so that "the informative-mask test can be run in any later session even if this disk does not survive" | `4e4fb05` committed **only** `MANIFEST.md` and `rasters/README.md`. Every one of the 260 `.tif` files was silently refused by `.gitignore:49` (`*.tif`), whose three negations covered neither path. The files existed in one working tree, on one disk, and in no repository. The rows also carried the wrong path: they omitted the `rasters/` prefix the files actually live under, so a verifier walking the manifest would have failed to find them even had they been tracked | `.gitignore` negation `!tubitak/docs/evidence/**` appended last; the 260 rasters committed and the manifest rows re-prefixed; 7 unlisted outputs given rows; fresh-clone verification; standing practice 10 gains its final clause (below) | [corrections-entry-35-draft.md](corrections-entry-35-draft.md) |
| 39 | 24 Aug | [letter-skeleton.md](letter-skeleton.md) §3 item I.5 and [related-work.md](related-work.md) §2 name **"Chen, Ohayon et al."** as the authors of *Looks Too Good To Be True: An Information-Theoretic Analysis of Hallucinations in Generative Restoration Models* (arXiv:2405.16475, NeurIPS 2024) | **No such paper exists.** The authors are **Regev Cohen, Idan Kligvasser, Ehud Rivlin and Daniel Freedman** (arXiv record and the NeurIPS 2024 proceedings page, checked 13 Sep). Neither Chen nor Ohayon is an author. The wrong names were written by the supervising session on 24 August and carried forward unverified through related-work.md, the skeleton, and on 13 September into the letter's Section I, Section V and `refs.bib` — a claim carried forward without verification, which is the class this paper is about, reaching the manuscript. Found by the supervising session on 13 September reading the compiled PDF; the drafting session's own report had flagged only that "the record did not list a full author line", which was the symptom | Citation corrected in the letter (`cohen2024looks`); every other entry in `refs.bib` verified against its DOI, DataCite, arXiv or proceedings record the same day and the verification recorded in the letter repository (`notes/citations-verified-2026-09-13.md`); the letter gains working rule 6, no citation without its record. Attributed to the supervising session | [related-work.md](related-work.md) §2, [letter-skeleton.md](letter-skeleton.md) §3 I.5 |
| 40 | 13 Sep | The letter's `refs.bib` entry for KARIOS (Zenodo 10.5281/zenodo.10598329) named the second author **"Canonicy, Jérôme"** | The DataCite record names **Canonicy, Patrice**. [related-work.md](related-work.md) §5 gives surnames only; the first name was supplied from memory by the drafting session on 13 September, and was wrong. Same class as entry 39, smaller, and this session's own | Corrected from the DataCite record in the same verification pass | letter repository `notes/citations-verified-2026-09-13.md` |
| 42 | 13 Sep | The letter's data-availability statement (draft of 13 Sep, gencp-letter `26092ad`–`58b540c`): "Training checkpoints and generated images are held in a private backup" | **The backup did not cover all of them.** The 24 epoch cells scored on 13 September for the training-time curve produced 24 × 520 generated images that exist on one machine only; the 26 August backup predates them. A statement vouching for material that is not where it says — **the class of entry 35**, in a manuscript rather than a manifest — found on the first end-to-end read the same day | Narrowed to what is backed up (images to 26 August; the generating, confirmatory and 96 epoch checkpoints, verified from the copy) with the exclusion stated in the sentence; gencp-letter P4 block 2. Terminology decision recorded here: "minimum-match-count sweep" is the letter's name for the point-floor re-scoring, used everywhere from block 5 | letter `06-data-availability.tex`; entry 35 |
| 44 | 13 Sep | [osm-render-baseline-registration.md](osm-render-baseline-registration.md) §(c): the pretrained arm's KLT rows on the Table I path "are not retained" | **They are retained**, at `tubitak/data/ankara/run/results/<stem>/`, and reproduce `turkey_karios.csv` on 130/130 chips. Asserted without a look; the third instance of one class after entries 35 and 42 — a statement about whether evidence exists, made without checking | Amended in the registration (`d2769d6`), the equal-count row constructed (`f27f710`): −1.778 ± 0.109 px over 123 chips, no reversal, reading unchanged. Class closed by the practice 10 extension of 13 Sep and by [absence-claims-audit-2026-09-13.md](absence-claims-audit-2026-09-13.md) | registration amendment; audit K |
| 45 | 26 Aug / 13 Sep | [evidence/rasters/README.md](evidence/rasters/README.md): if the per-seed arm warps are lost "the arm outputs must be re-inferred" | Overstated: every seed's generated images are in Kaggle backup-2 (`generated_fakes.tar` members listed 13 Sep: 520 per Modal seed), and the warps are regenerable from them by the registered affine. Re-inference is needed only if the backup is lost too | Dated note at the sentence; no number changes | audit F |
| 46 | 21 Aug / 13 Sep | [tool-results.md](tool-results.md):139: Package A "has no artifacts in this repository" | True when written, stale since `evidence/pkgA/` was committed on 26 Aug; a reader of the sentence today would conclude the artifacts are absent | Dated note at the line; no number changes | audit TR |
| 47 | 26 Aug / 13 Sep | The 130 Ankara evaluation inputs "cannot be regenerated" because they were rendered from a live map API "with no dated archive" (BACKUP.md, addendum §13, the letter's data-availability statement) | **Asserted, never tested.** The Overpass QL `[date:…]` setting queries attic data as of a past instant (documented; attic since 2012), and the working machine holds 68 cached Overpass responses. Whether a byte-identical regeneration is possible is unknown; what is known is that the renders exist on disk, in backup-1 and, warped, in the repository. Genuinely ambiguous, and now said so | Manuscript sentence replaced by what is known; BACKUP.md and addendum left as written with this entry as the correction | audit I |
| 48 | 27 Aug / 13 Sep | [evidence/MANIFEST.md](evidence/MANIFEST.md): the 260 raster rows name `input_render_warped/<stem>.tif` and `real_chip_bt601/<stem>.tif`; entry 35's remediation table says the rows were re-prefixed to `rasters/...` | **The re-prefix landed and was reverted.** `284571b` (26 Aug) prefixed the rows; the snapshot refresh `6750978` (27 Aug) — the same commit that deleted entry 35's draft — copied a manifest without the prefix, while the files stayed under `rasters/`. From 27 August to 13 September every raster row pointed at a path that did not exist in the repository. Found by the presence-claims audit's stranger check: 260 of 399 manifest rows failed by path, all 260 files present at `rasters/` with matching hash and size | Rows re-prefixed; path note in the manifest; the audit's fresh-clone re-check after the fix is recorded in [presence-claims-audit-2026-09-13.md](presence-claims-audit-2026-09-13.md). Entry 35's defect, restored by housekeeping: the fourth instance of the class practice 12 was minted for. **Mechanism, 13 Sep (P9 A.1):** `scripts/manifest_paths_check.py` runs in the pre-commit hook on every commit and refuses any commit whose manifest names a path not in the index; practice 10 carries the note | audit §2 |
| 49 | 13 Sep | [osm-render-baseline-results.md](osm-render-baseline-results.md), the letter's III-L and EVIDENCE.md: the render's median surviving matches per chip is "30" | The value in `render_baseline_summary.json` is **29.5** (an even count of chips; the median falls between two values). The results document's format string rounded it and the manuscript copied the rounding | 29.5 stated in all three places; nothing else changes | audit A.5 (OSM render) |
| 50 | 13 Sep | [evidence/MANIFEST.md](evidence/MANIFEST.md) carried two rows for `osm_render_baseline/render_baseline_summary.json`, the first with the `c263e4c` hash | The committed file is the `f27f710` version; the first row's hash no longer matched anything in the repository and a stranger verifying it row by row failed it | First row marked superseded, hash kept for the record | audit §2 |
| 51 | 14 Sep | [paper-context-addendum.md](paper-context-addendum.md) §19 and the letter's II-F: the 1/256 scale error "explains roughly 3.9 % of the reported variance" | **Underived.** From the two printed inputs, predicted σ 2.89 m against reported 14.5–17.3 m, the share is (2.89/14.5)² = 4.0 % down to (2.89/17.3)² = 2.8 %; no computation in the record yields 3.9 %. Found by the P10 number audit | The letter states the range with the computation inline; the addendum carries a dated note. The sentence's job — the error is too small to overturn the published conclusions — is unchanged | P10 audit (c) 1 |
| 52 | 14 Sep | Letter III-L: the equal-count truncation "leaves each fine-tuned comparison within 0.02 px of its raw value" | The number appeared in the letter only, with its aggregation unstated: the six-seed mean per arm moves by ≤ 0.019 px, but individual seed-arm comparisons move by up to 0.082 px. Fourfold understatement of the spread, none of it material against advantages of 0.44–1.22 px | Both quantities stated with the aggregation named, in the letter and in [osm-render-baseline-results.md](osm-render-baseline-results.md) | P10 audit (c) 3 |
| 53 | 14 Sep | Letter IV-C and Table II row 3: the two matcher registrations "share no chip set"; "the arm ordering is preserved in every condition cell except two"; the MI grid "censors 15.8 % of chips" | Three drifts from the artifacts. Both registrations were scored on the same four chip sets and the same seed-42 images (`B3_scores.csv`, `pkgA_scores.csv`). The record says L1-only ranks first *or ties C3 within noise* in every cell but two; `pkgA_summary.json` carries six further rank-unstable cells, none displacing L1-only. 15.8 % is 147 of 930 MI *measurements* over four sets; the Ankara set the paragraph discusses is 70 of 390, 17.9 % (pretrained 36/130, C1 23, C2 11) | Row and section rewritten from the artifacts: shared chip sets stated and explained as harmless (the matcher is the factor under suspicion and the matcher varied); the ordering claim narrowed to the record's; the Ankara censoring figure with the correct unit | P10 audit (c) 4–5 |

**On entry 35's draft.** The draft of entry 35 was itself deleted: created as a loose file
in `284571b`, removed by the snapshot refresh `6750978` the next day, and reported on
13 September by a session that searched the record as never having existed. It was
recovered from history (`a415e25`) and is applied above. The failure is the class entry 35
records, one level up — a drafted correction about evidence that went missing, itself
lost without anyone noticing — and the loose-file form is why: a drafted entry is read by
nothing yet, so it passes the "does anything read it?" check by construction. A standing
practice for where drafts live is proposed, not yet minted.

## What would have caught each sooner

1. Reading KARIOS's own definition of `mean_x`/`mean_y` before comparing to it.
2. Not silencing stderr in a harness, ever; an assertion that every gate chip has a reference file.
3. Reading the optimizer construction (`optimizer_G`/`optimizer_D` share one `--lr`) before writing a rate-ratio justification.
4. Checking scene availability for all sites at registration time, not at preparation time.
5. Printing the effective LR per epoch in the training log (one line: `lambda_rule`'s output); any glance at it shows a zero. The symmetry claim would have been caught by writing the per-arm schedule out epoch by epoch — five minutes of arithmetic — before using it in a decision.
6. Byte-level comparison gates on ANY substituted data source, run before adoption (this is now the standing "transparency gate" pattern); reading osmium's docs on extract strategies rather than assuming config keys are honored.
7. Preflight from inside the running kernel (which is what caught it); sooner: never hard-coding a mount path the platform does not document as a contract.
8. Reading the CLI's slug-derivation warning as an error, not a suggestion.
9. A one-line CUDA smoke test (`torch.randn(2,2,device='cuda') @ ...`) in preflight instead of trusting `is_available()`; capability-vs-arch-list assertion now standard.
10. Copying any about-to-be-touched untracked file aside before running a tool that writes into its directory.
14. Reading the inference options against the model definition once: `not opt.no_dropout` + no `model.eval()` is two lines of code; and running any command twice and diffing before calling it "byte-verified". The pix2pix paper states the design in one sentence.
15. The regenerable-label audit that is now on the Phase F backlog — re-rendering each corpus input at gate time would have caught the stale renders the day the fix landed; recording render-path provenance beside every corpus artifact (the tool now does this) prevents the class.
16. Citing, in every results table, the config file of the runs it came from — the KARIOS config JSON sits beside every output; diffing that JSON against the governing registration before writing the table is one command. And treating a failed fix exactly like a failed run: E3-a's failure was disclosed, but the replacement configuration needed its own registration (or its own retrospective label) before its numbers were written down.
17. A completeness pass over the registration text at results-writing time: every registered protocol element either has an artifact or has a disclosed absence. The ORB omission survived because the results document was written from what ran, not diffed against what was registered.
19. The same completeness pass as 17, applied to *reporting form* and not only to execution: the registration named two band conversions and a distribution, the document carried one conversion and three medians. "Which registered outputs does this document not contain?" is a different question from "which registered runs happened?", and both must be asked.
20. Asking of every summary statistic: **what value could this have returned if the hypothesis were true?** An adjusted mean at the covariate means is the raw mean by construction; five minutes of algebra, or one synthetic covariate of pure noise as a negative control, exposes it. A mediation test that returns exactly 0% on three different covariate sets is reporting its own identity, not a result.
21. One `assert` that the refined output is not on the search lattice — or a single glance at the value column, where 930/930 integer hypots are visible without any tooling. A registered refinement step that leaves no trace in the numbers did not run.
22. Committing the harness beside the results, which entry 16's postscript already prescribed and the C4/C5 package adopted; and never letting a follow-up package extend an earlier package's scripts in place — copy them aside first (entry 10's lesson, unlearned).
23. A check at commit time that **every document a registration, an amendment or a results file links to is tracked** — one `git ls-files --error-unmatch` per link target. The handoff document had been relied on for two days as if it were in the record; nothing surfaced it until an amendment tried to link to it. Untracked is invisible: `git status` shows it once, in a list nobody re-reads.
24. Generating results tables **from** the summary artifact rather than retyping them beside it: every one of these three defects is a transcription step a reader cannot see. A one-line assertion that each printed value round-trips against its JSON field would have caught all of them, and the unreproducible SE column would have failed loudly instead of silently.
25. Writing a per-chip CSV for every scored package, without exception — the same convention B1, B2 and C45 followed in the same repository. Phase C is the earliest package and predates the habit; it is also the one the paper's primary comparison is registered against, which is the worst possible place for the gap.
26. Registering stop rules with a **threshold and a single governing test**, not a two-part rule with an unquantified verb. "Rising" fires on any positive difference; nobody intended that, so nobody applied it, and the rule silently became whatever the sharp half said. A rule that is never applied literally is not a rule — it is a note.
27. Separating the two questions a failed gate raises — *was the rule right?* and *did we say what happened?* — and answering them in that order, in different places. Entry 26's defence is sound and entry 27's error is real; had they stayed one entry, the defence would have read as an excuse for the misstatement, and a reader would have been right to distrust both.
28. A check that **every registered gate names both a threshold and the code that evaluates it**. A gate specified in prose alone is not a gate; it is an intention. Two packages carried this one as an active divergence test, and the question that exposed it — "what number does it fire at?" — takes one minute to ask of any registration and had never been asked of this one.
29. **Every property the invariance table asserts needs a corresponding preflight capture, or the assertion is unfalsifiable.** That is the lesson across all of this entry's instances — image version, enumeration order, and arguably the seed-42 code path: each was asserted as held and none was recorded, so none could be checked afterwards. An invariance that cannot be checked after the fact is not an invariance, it is a hope. `pip freeze` and the ordered file-list hash are the first two implementations, both one line each. The tell was that the invariance table named a specific torchmetrics version - precise enough to look verified, while everything around it went unrecorded.
30. Emitting the cell total from the harness, and generating the "N of M" sentence from the summary artifact rather than counting by hand — entry 24's lesson applied to a count instead of a value. The unreported second exception would have surfaced from any per-cell listing of ordering verdicts.
31. Naming the registration each number in a table row comes from, in the cell. A row that quotes numbers from two registrations without saying so reads as one test; a provenance column makes the fusion visible to its author before a reviewer.
32. Standing practice 10, which this entry produced: an artifact a published number depends on is committed with its script, and "regenerable" is a claim checked against `git ls-files`, not asserted.
33. The completeness pass of entries 17 and 19, run against the registration's *reporting* requirements at results-writing time: every named set and every required per-stratum output either has a line in the results document or a disclosed absence.
34. The same pass, run against the sentence "run and reported before this document was written": a claim that checks 1–7 are reported is verified by locating each of the seven in the document that makes the claim.
35. **After an evidence commit, verify from a fresh clone that the files are actually there.** `git add` exits zero after adding nothing when the path is ignored; `git status` is silent about ignored paths; `git log` reports what was committed, not what was intended. A file is evidence when a stranger can obtain it.
36–38. Quoting the implementing code's expression of every registered set, threshold or condition in the registration itself (standing practice 11), so prose and code sit in one place and drift is visible by reading.
39. Verifying a citation against its DOI or arXiv record at the moment it is first written down, not at submission. The names were wrong for twenty days across three documents and nobody had opened the record; the check takes one request per entry and is now scripted.
40. The same check; and never supplying a bibliographic detail the source document does not contain.

**Entry 13 addendum — what would have caught it sooner:** recording the input raster's
provenance alongside every output. The tool embeds provenance in its outputs, which is why
this class of error cannot recur in the production path. Related incident, same date: the
session scratchpad was purged and every artifact labelled "regenerable" in the results
documents had to be regenerated; regeneration succeeded this time, but a regeneration path
can drift. Added to the Phase F backlog: **audit the "regenerable" label** — for every
artifact a document depends on and does not commit, verify it regenerates to the same bytes
with the generation path pinned. An artifact we cannot reproduce at report time is an
artifact we cannot defend.
