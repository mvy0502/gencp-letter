# Phase F backlog

- **Veto rule re-registration** — inherit the recorded failure mode
  ([gcp-veto-rule-results.md](gcp-veto-rule-results.md)): site-conditional loss in the
  selection criterion, probability-calibrated operating point, fresh held-out evaluation.
- **Audit the "regenerable" label** (from corrections entry 13 addendum): for every
  uncommitted artifact a results document depends on, verify byte-identical regeneration
  with the generation path pinned.
- **Panchromatic/single-band output** — supplied by Package A; `--bands single` in
  gencp-ref stays refusing until then.
- **Sparse-Europe regression under C3** (+0.235 px, phase-c3-results.md) if European
  deployment becomes a goal.
- **Retrain on production-provenance (post-fix Geofabrik) inputs** — closes the
  measured train/serve skew (~0.6 px forest-heavy); see standing-practices.md item 3.
